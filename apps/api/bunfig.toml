# Тот же preload, что и в корневом bunfig: без него `bun test` из apps/api
# берёт рабочую базу вместо тестовой и падает девятью тестами, а из корня всё
# зелено — самая неприятная разновидность непонятной ошибки.
[test]
preload = ["./test/preload.ts"]
