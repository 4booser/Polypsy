import { useLang } from "../../lang";
import { ButtonLink } from "../../ui/primitives";
import { PublicFrame } from "./PublicFrame";

/*
 * Лендинг «Про кампанію» — кадр f00. Что видит тот, кто открыл корень, не
 * войдя: до этого корень отдавал сразу форму входа, и шага «прочитал → нажал
 * «Увійти» → попал на вход» в системе не было.
 *
 * Под знаком (его рисует рамка): заголовок 32/700, абзац 20/400 с
 * интерлиньяжем 29 в колонке 510 — ровно ширина текста на кадре, при которой
 * строки ложатся так же, — и белая кнопка 215 × 45 с «Увійти» 22/700 — та же
 * кнопка формы, что и в консоли, только белой плашкой на сиреневом (вариант
 * paper у Button), потому что это ссылка на экран входа, а не действие.
 *
 * Отступы сверху вниз — с кадра: 57 от знака до заголовка, 41 до абзаца, 33
 * до кнопки. Считаются от коробок строк с интерлиньяжем 44 и 29, поэтому
 * выглядят не круглыми: круглить их значило бы сдвинуть текст на кадре.
 *
 * Интерлиньяж заголовка 44, а не 43: на f01 тот же заголовок идёт в две
 * строки, и шаг между ними замерен дважды — по базовым линиям (401 и 445) и
 * по верху строчных (384 и 428), оба раза ровно 44. Заголовок здесь
 * однострочный, поэтому его коробка выросла на 1, и отступ до абзаца стал
 * 41: первая базовая линия абзаца как стояла на 474, так и стоит (строки
 * кадра 459…478, 488…507 … шагом 29).
 */
export default function Landing() {
  const { ut } = useLang();
  return (
    <PublicFrame>
      <section id="about" aria-labelledby="about-title" className="mt-[57px]">
        <h1 id="about-title" className="m-0 text-[32px] font-bold leading-[44px] text-primary">
          {ut("pub.about")}
        </h1>
        <p className="m-0 mt-[41px] max-w-[510px] text-[20px] leading-[29px] text-primary">
          {ut("pub.aboutText")}
        </p>
        {/*
          Белая плашка 215 × 45 с «Увійти» 22/700 — та же кнопка формы, что и
          в консоли (sizes.md + variants.paper), только ссылкой: она ведёт на
          экран входа, а не совершает действие.

          Классами вручную это было набрано до сверки: высота 45, кегль 22,
          радиус 5 и белая заливка лежали и здесь, и в primitives.tsx — на
          кадре f00 и на кадре f01 плашка одна и та же, а в коде их было две.
          Ширина остаётся здесь: у кнопки её нет как свойства (см. `sizes`).
        */}
        <ButtonLink to="/login" size="md" variant="paper" className="mt-[33px] w-[215px]">
          {ut("lg.signIn")}
        </ButtonLink>
      </section>
    </PublicFrame>
  );
}
