# Quizzy — дорожная карта (живая копия)

> Хозяин файла — репозиторий; отметки статуса обновляются по мере работ.
> Исходный план составлен 2026-08-26. ✅ готово · ⏳ частично · ⬜ не начато


## Контекст

Quizzy — система психодиагностики для военно-медицинского учреждения: API (Bun + Hono +
Drizzle + PostgreSQL 16), веб-консоль (Vite + React 19), мобильное приложение (Expo SDK 57),
общий пакет с движком подсчёта. ~17k строк TS. Уже реализовано: версионирование методик,
ключевой подсчёт (K-коррекция, T-баллы, стены, шкалы достоверности), группы и скоупинг
админов (`lib/scope.ts`), журнал доступа включая чтения, тревоги риска с эскалацией,
потоковая телеметрия ответов, психометрия (альфа Кронбаха, item-total), сравнение когорт,
корреляции, SPSS-экспорт, батареи со строгим порядком (серверная проверка), расписания
повторов с идемпотентным исполнителем, печатные бланки и ключи, 4 реальные методики.

Решения пользователя при планировании:
- **Развёртывание: облако/VPS** — можно email/push и внешний мониторинг; выше требования к защите.
- **Приоритет после фундамента: масштаб и полировка** (киоск, приглашения, i18n, рефакторинг).
- **Защита данных: разумный максимум** — всё, что возможно без внешних аудиторов.

Сквозной принцип всей карты: каждый новый фоновый процесс — по образцу `lib/scheduler.ts`
(идемпотентный тик + журнал запусков), каждое действие — в `AuditAction` (`lib/audit.ts`),
каждая выборка — через `surveyScopeFilter`/`assertGroupAccess`, каждый расчёт — чистой
функцией в `packages/shared` с золотым тестом.

---

## Найденные дефекты — ✅ все 8 закрыты

1. **Репозиторий не под git** — ни одного коммита. Потеря диска = потеря всего проекта.
2. **Дыра авторизации:** `apps/api/src/routes/access.ts:129` — `GET /api/access/patients`
   отдаёт ВСЕХ пациентов (имя, email, подразделение) любому групповому админу без
   фильтра `accessibleGroupIds` и без аудита. Патч: фильтровать по доступным группам + журнал.
3. **Неверное действие в журнале:** `users.ts` пишет `"user.create"` при смене роли
   (`PATCH /:id/role`) — завести `"user.role_change"`.
4. **Мёртвое объявление** `"response.draft"` в `AuditAction` — нигде не эмитится; убрать
   или начать логировать.
5. **CORS `*`** при токенах в localStorage — сузить до allowlist из env.
6. **`GET /health` не проверяет БД** — вернёт ok при упавшем Postgres; добавить `select 1`,
   разделить liveness/readiness. Плюс: «планировщик тикал < 2 ч назад» по `schedule_runs`.
7. **Stop-колбэк планировщика выбрасывается** (`index.ts:59`) — сохранить и вызывать при
   SIGTERM.
8. **Нет смены/сброса пароля вообще** — заблокированного пользователя чинят руками в БД.

---

## Этап 0 — Фундамент (≈3 недели)

### 0.1 Git + гигиена (S) — ✅
`git init`, первый коммит, проверить что `.env` вне индекса; `LICENSE` в корень,
`SECURITY.md`, `CHANGELOG.md`. Дальше — коммит на каждый пункт плана.

### 0.2 Тесты — движок подсчёта (L) — ✅ 86 тестов
Ноль тестов на 366 строк психометрики; ошибка в `computeProfile` = молча неверные
клинические баллы. Раннер — `bun:test` (родной, без зависимостей).
- **`packages/shared/src/scoring.test.ts`** — золотые протоколы из пособий:
  Мини-мульт (муж/жен: сырые → K-коррекция → T), СР-45 (доля, инвертированная градация,
  шкала лжи на 0.6), МЛО (инвертированные стены, композит ЛАП), обратные пункты на краях,
  порядок «все сырые → коррекции» на взаимозависимых шкалах, `normFor` (выбор по
  полу/возрасту, fallback, отсутствие → warning), `isQuestionVisible`/`isAnswered`.
- **`validate.test.ts`** — по тесту на каждый класс Issue.
- **`apps/api/src/lib/stats.test.ts`** — `cronbachAlpha`, `pearson` на эталонных матрицах.
- **API-интеграционные** через `app.request()` (без сети) + тестовая БД `quizzy_test`:
  скоупинг (групповой админ не видит чужого — регресс на дыру №2), атрибуция
  subjectId при onBehalfOf, строгий порядок батареи (пациенту отказ / специалисту можно),
  автозакрытие назначений, идемпотентность `runDueSchedules` (два прогона — нет дублей),
  версионирование (PATCH создаёт версию, старые прохождения держат свою), `detectRisks`.
- Переиспользуемая часть сида — вынести из `seed.ts` для тестовых фикстур.

### 0.3 CI (S) — ✅
`.github/workflows/ci.yml`: install → typecheck 3 приложений → `bun test`
(postgres:16 в services) → `vite build`. + Renovate/Dependabot еженедельно;
`bun audit` шагом CI. Позже — шаг сборки Docker-образа и прогон целостности контента
(обёртка над `validateSurvey` по всем опубликованным версиям сида).

### 0.4 Аутентификация (M) — ✅ (refresh, lockout, смена пароля)
- Access-токен 30 дней → **15–30 минут**; opaque refresh-токен хэшем в таблице
  `refresh_tokens` (ротация при каждом обновлении, ревокация семейства при краже/логауте);
  `POST /api/auth/refresh`, `POST /api/auth/logout`; отзыв всех refresh при смене
  роли/пароля. Клиенты: перехват 401 → тихий refresh → повтор (в `web/src/api.ts` и
  `mobile/src/api/client.ts`).
- Rate limiting: логин 5 попыток/мин на email+IP + lockout со счётчиком (`auth.lockout`
  в журнал), общий лимит на IP. In-memory достаточно для одного инстанса.
- Смена пароля (`POST /api/auth/password`), сброс суперадмином с одноразовым паролем и
  `users.mustChangePassword`; форс-смена дефолтных паролей сида.
- Политика: ≥12 символов, проверка по топ-10k утёкших (локальный список), zod-схема в shared.

### 0.5 Транспорт и процесс (S) — ✅
`secureHeaders()` (CSP, HSTS, X-Frame-Options), CORS-allowlist, bodyLimit ~1 МБ,
request-id middleware (в ответ и в журнал), graceful shutdown (SIGTERM → стоп планировщика
через существующий колбэк → дождаться пула → выход), TLS к Postgres по env-флагу,
убрать дефолт `postgres://postgres@localhost` в production (fail-fast как у JWT_SECRET),
`env.ts` переписать на zod-валидацию всех переменных.

### 0.6 Защита данных (M) — ⏳ хэш-цепочка ✅, бэкапы ✅, шифрование полей ✅; согласия, ретенция — впереди
- **Хэш-цепочка журнала:** `audit_log` + `seq`, `prevHash`, `entryHash`; писать в
  `audit()`/`auditSystem()` (единственные точки записи). `GET /api/audit/verify`
  (суперадмин) + `bun run audit:verify`. На уровне БД: `REVOKE UPDATE, DELETE` на
  `audit_log` у роли приложения. Периодическое якорение головного хэша наружу (файл/печать).
- **Шифрование полей:** `lib/crypto.ts`, AES-256-GCM (WebCrypto), keyId в шифртексте для
  ротации. Кандидаты: ФИО/дата рождения `users`, свободнотекстовые `answers`, будущие
  заключения. Числовые баллы не шифровать (SQL-аналитика). Для поиска по имени —
  blind-index (HMAC). Тела запросов убрать из логгера.
- **Согласие:** таблица `consents` (userId, версия текста, дата, IP), экран при первом
  входе пациента, текст редактирует суперадмин.
- **Ретенция:** `answer_events` сворачивать в агрегаты через N месяцев (джоб по образцу
  планировщика), журнал не удалять — партиционировать по месяцам; политика в конфиге.
- **Бэкапы:** `scripts/backup.sh` (pg_dump + шифрование age/gpg + ротация 7/4/12),
  `scripts/restore.sh`, процедура восстановления в RUNBOOK, периодическая проверка восстановления.

### 0.7 Развёртывание (M) — ✅ (Docker, compose, Caddy; мониторинг — позже)
Multi-stage Dockerfile API (oven/bun → slim, non-root, HEALTHCHECK, миграции init-шагом),
сборка веба → статика за Caddy (TLS автоматом), `docker-compose.yml`
(postgres+volume, api, web, caddy, mailhog для dev). Мониторинг: self-hosted
GlitchTip/Sentry в `app.onError` + web + mobile; структурные JSON-логи; uptime-kuma
по желанию.

### 0.8 Планировщик и реплики (S) — ✅
`pg_advisory_lock` вокруг `runDueSchedules` (идемпотентность через `schedule_runs` уже
есть — лок второй пояс) + флаг `SCHEDULER_ENABLED`, чтобы тикала одна реплика.

### 0.9 RLS (L) — ⬜ не начат
Политики Postgres на `responses`, `answers`, `response_scores`, `risk_alerts`, `surveys`
по group_id; `SET LOCAL app.user_id/app.role` в обёртке транзакции (`db/index.ts`);
приложение под non-superuser ролью; планировщику и `auditSystem` — отдельная system-роль.
Логика зеркалит `accessibleGroupIds`. Таблица за таблицей, с интеграционными тестами 0.2.
Это страховка от забытого `assertSurveyAccess`, не замена ему.

---

## Этап 1 — Масштаб и полировка (приоритет пользователя, ≈4 недели)

### 1.1 Ссылки-приглашения (M) — ✅
Таблица `invites` (хэш токена, createdBy, groupId?, batteryId?, expiresAt, maxUses,
usedBy[]). Админ создаёт ссылку + QR (SVG на месте, без библиотек); пациент открывает
`/join/<token>` → регистрация (обычная или псевдонимная — логика псевдонимов уже есть)
с автопривязкой назначений. Регистрация по приглашению обходит правило «первый — админ»;
прямую регистрацию пациентов закрыть env-флагом. Аудит `invite.create`/`invite.use`.

### 1.2 Киоск-режим (L) — ✅
Один планшет на поток (призыв, ВЛК).
- Веб-роут `/kiosk/<sessionToken>`, отдельный layout: оператор создаёт «сеанс»
  (таблица `kiosk_sessions`: батарея/методика, группа, оператор, expiresAt) → цикл на
  киоске: «введите личный номер / выберите себя / код приглашения» → прохождение →
  «передайте планшет следующему».
- Fullscreen API, history-trap от навигации назад, таймаут бездействия со сбросом на
  стартовый экран, крупные шрифты. На планшетах — guided access / screen pinning
  (задокументировать в RUNBOOK).
- Сеансовый токен ограничен одной сессией и сдачей прохождений — никаких прав оператора
  в браузере киоска. Каждый вход/выход участника — в журнал.
- В консоли — живой прогресс сеанса (кто прошёл/в процессе, поллинг).

### 1.3 i18n оболочки (M) — ✅ пациентские поверхности; консоль — русская намеренно
Контент двуязычный, оболочка — хардкод русского в обоих клиентах и в ошибках API.
- `packages/shared/src/ui-strings.ts` (`{ key: { uk, ru } }`) + хук `useT()` — без
  i18next, ~400 строк. Выбор языка в профиле + автоопределение.
- Ошибки API → коды (`{ error, code, requestId }` — заодно единый формат), клиент
  переводит; фолбэк — текущий русский текст. Печатные отчёты — на языке контента.

### 1.4 Конструктор (M) — ✅ (модули, массовая вставка, автосейв, undo)
`Constructor.tsx` (891 строка) → `pages/constructor/`: QuestionList, QuestionEditor,
ScaleEditor, NormsEditor, BandsEditor, LogicEditor, SurveyMeta + контекст черновика.
Ориентир разбиения — mobile `src/components/builder/*` уже разбит так.
Новое: undo/redo (стек снапшотов), дублирование вопроса, перетаскивание порядка,
**массовая вставка пунктов из текста** («1. Текст…» построчно → вопросы да/нет —
главный сценарий переноса методик из пособий), автосейв черновика в localStorage,
beforeunload-предупреждение, Ctrl+S. Делать после дымового E2E (1.7), чтобы не сломать молча.

### 1.5 Производительность (M) — ✅ (кэш версий 142→8 мс, пагинация, gzip)
Курсорная пагинация по образцу `routes/audit.ts` на: `/surveys/:id/responses`, `/users`,
`/access/patients`, `/dynamics/respondents`, `/alerts?all=1`, `/batteries`. DataTable —
«загрузить ещё». `analytics.ts` — агрегаты (распределения, воронка, средние) перевести
в SQL, в JS оставить психометрию (альфе нужна матрица); период по умолчанию — год.
Индексы: `(survey_id, status)` на responses, `(response_id, scale_id)` на response_scores,
`(survey_id, created_at)` под пагинацию; EXPLAIN основных запросов. Компрессия ответов
(hono compress). TTL-кэш резолва локализованного контента по versionId (контент иммутабелен).

### 1.6 Импорт методик (M) — ✅ (каталог-надстройка — позже)
`POST /api/surveys/import` — JSON из экспорта, `formatVersion`, ремап всех id
(вопросы/опции/ключи/коррекции/логика/стены — таблица соответствий старый→новый),
прогон `validateSurvey` с отчётом Issues ДО сохранения, всегда новая версия. UI:
страница импорта с drag&drop и предпросмотром проблем. Надстройка-«каталог»: манифест
пакета (версия, контрольная сумма, статус выверки, источник), встроенные
`apps/api/src/instruments/*` как предустановленные пакеты в один клик.

### 1.7 E2E-смоук (M) — ⬜ требует браузера, гонять в CI
Против compose-стенда с сидом: «логин → назначить → пациент прошёл (через API) →
профиль и тревога видны», «создать методику в конструкторе → опубликовать»,
«батарея по порядку». Отдельный job CI. Заодно axe-core прогон (доступность).

---

## Этап 2 — Клиническая ценность (≈4 недели)

### 2.1 Уведомления о тревогах (M) — ✅ (email+эскалация; push — позже)
Сейчас тревога видна только при открытой консоли — для суицидального риска неприемлемо.
- Email (SMTP из env, шаблоны uk/ru): немедленно при тревоге — админам группы
  (`groupAdmins`); дайджест открытых раз в день. Без ПДн в письме — только «тревога по
  методике X в группе Y».
- Эскалация: `alertEscalateMinutes` уже в схеме — тик по образцу планировщика (1–5 мин)
  шлёт повторно суперадмину, если не подтверждена. Идемпотентность — `alert_notifications`.
- Push (expo-notifications + Expo Push API, таблица `push_tokens`): пациенту — о
  назначении/сроке (отправка из `runDueSchedules`), напоминание о незакрытой батарее;
  специалисту — о тревоге. Настройки per-user (`notification_prefs`).
- Все отправки — в журнал (`alert.notified`, `alert.escalated`).
- Дешёвая полумера до внедрения: favicon-бейдж и звук на странице тревог (поллинг есть).

### 2.2 Офлайн-режим мобилки (L) — ✅
Локальная БД (expo-sqlite) — источник истины: кэш полной версии назначенной методики
(контент версии иммутабелен — версионирование это гарантирует), локальный черновик,
очередь мутаций (ответы, события, сдача) с ретраями/backoff; NetInfo → синк; конфликтов
почти нет (append-only, один пациент — одно устройство). Тайминги пишутся локально в
момент событий — сервер уже принимает их как есть. Идемпотентный upsert черновика по
клиентскому id. Индикатор «не синхронизировано: N», запрет логаута при непустой очереди.
`computeProfile` в shared — завершение показывается офлайн; при синке серверный пересчёт
сверяется и расхождения логируются (их быть не должно — общий код).

### 2.3 Заключения (M) — ✅ (печать через браузер; серверный PDF — позже)
- Таблица `conclusions` (responseId, текст специалиста, статус draft/signed, версии,
  кто/когда) — правки поверх автоинтерпретации; подпись фиксирует снапшот, дальше —
  новая версия; всё в журнал (`conclusion.sign`).
- PDF: первый шаг — полноценный `@media print` CSS у HTML-отчёта (паттерн уже отработан
  в BlankForm/KeyPrint) + печать в PDF из браузера; серверный рендер (headless chromium в
  контейнере) — вторым шагом, если понадобится автоматизация.
- Пакет: «все заключения сеанса киоска / подразделения за период — одним PDF/zip».
- Шапка учреждения из настроек; в печати — дата, страница, кто распечатал (для подшивки).

### 2.4 RCI (M) — ✅
Стандарт measurement-based care (Jacobson–Truax): SEM = SD·√(1−r); r — фактическая
альфа (уже считается), SD — из норм шкалы (`scaleNorms`) или по выборке.
`reliableChange()` в `packages/shared` с золотым тестом → в `routes/dynamics.ts`
помечать пары замеров «достоверное улучшение / в пределах ошибки / ухудшение» →
бейджи на графиках динамики, строка в отчёте повторного замера, в сравнении когорт
«до/после» — доля надёжно улучшившихся.

### 2.5 Safety-план (S) — ✅
В настройках методики — текст немедленных действий (телефоны, дежурный): показывается
пациенту сразу после сдачи при riskFlag и специалисту в тревоге. Чистая конфигурация.

### 2.6 «Моя динамика» (S) — ✅
Флаг `showResultsToPatient` (по умолчанию выкл): график своих стенов/T по замерам,
без клинических формулировок.

---

## Этап 3 — Контент методик (параллельно, порциями)

### 3.1 Сверка ключей (M, руки + печать)
По `/surveys/:id/key` сверить все 4 методики с пособиями; расхождения — коммитами с
указанием страницы. ЛАП-стены МЛО (таблица выведена суммированием) — проверить по
оригиналу. 10 украинских пунктов МЛО с null (строки 197–206 `mlo_items.ts` — как раз
алкоголь/агрессия/неподчинение, клинически нагруженный хвост) — выверить.
Результат фиксировать: поле `verifiedAt`/`verifiedBy` на версии + бейдж «выверено»
в консоли; золотые протоколы из пособия — в тесты (0.2).

### 3.2 Новые методики из пособия НПС (M каждая)
Через массовую вставку пунктов (1.4) + `validateSurvey` + печать ключей + сверка +
золотой тест. Порядок — по клинической востребованности заказчика.

### 3.3 Правовой статус (S) — ✅ docs/INSTRUMENTS.md + isDemo
`docs/INSTRUMENTS.md`: источник, страницы, статус прав по каждой методике (README уже
честно фиксирует, что тексты требуют очистки прав перед клиническим применением).
Демо-методики пометить флагом `isDemo` и скрывать из клинических списков.

---

## Этап 4 — Исследовательский уровень (по мере накопления данных)

- **Локальные нормы:** страница «пересчитать нормы по выборке» (M/SD с фильтрами
  пола/возраста, предпросмотр «как изменились бы T» прогоном `computeProfile` с
  кандидатными нормами), публикация = новая запись норм с датой/источником — версии
  уже это умеют. Пометка «локальная норма» в отчётах.
- **IRT:** офлайн-оценка 2PL (R/mirt по SPSS-экспорту), хранение параметров пункта,
  страница item-анализа рядом с альфой. **CAT** (адаптивное предъявление МЛО-200 —
  сокращение вдвое) — только после калибровки и реальной потребности, отдельный план.
- **Исследовательский экспорт:** профили деидентификации, codebook.csv, хэш датасета в
  журнал, CSV/parquet для R/Python рядом со SPSS.
- **Федерация госпиталей** (если появится второй узел): обмен только агрегатами —
  нормами и пакетами методик через каталог (1.6), без сырых ПДн.

---

## Мелочи (S каждая, пачками между этапами)

Веб: document.title по странице · хлебные крошки везде (PageHead умеет) · горячие клавиши
(`/` поиск, Esc закрыть) · фильтры/сортировка таблиц в URL · «скопировать ссылку» на
пациента/методику/ответ · пустые состояния с кнопкой-действием · подтверждение удаления
печатанием названия · тема по системной с ручным оверрайдом · автообновление тревог
раз в 60 с + индикатор устаревания · CSV-экспорт любой таблицы (клиентский, из DataTable) ·
печать профиля пациента одним документом · инициалы-аватарки в списках · фильтр по
подразделению в пациентах и тревогах · диапазон дат в аналитике · диф двух версий
методики · «кто сейчас проходит» (черновики моложе 30 мин) · git sha сборки в футере.

API: OpenAPI JSON + Swagger UI за staff-логином · soft-delete методик (`deletedAt`)
вместо каскада по прохождениям, жёсткое удаление отдельной командой суперадмина ·
`GET /api/stats/storage` (размеры таблиц, рост журнала) · zod-валидация query-параметров ·
`surveyVersionId` в каждую строку SPSS-экспорта (воспроизводимость: какие ключи
применялись).

Мобилка: уважать системный масштаб шрифта, цели ≥44pt, accessibilityLabel на вводе ·
экран «нет сети» с ретраем (до 2.2) · «осталось ~N минут» из медианной длительности ·
прогресс «тест 2 из 4» в батарее · подтверждение выхода из незавершённого теста ·
автоскролл к первому неотвеченному при завершении · хаптика при выборе ответа ·
биометрический вход (expo-local-authentication).

Данные/инструменты: `bun run db:reset` (dev-only, защита NODE_ENV) · демо-учётка
read-only для показов · сид с пропусками и «too fast» для демонстрации флагов качества ·
CI-проверка целостности контента всех опубликованных версий.

Документация: `docs/ARCHITECTURE.md` (mermaid-схема таблиц, инварианты: порядок
подсчёта, версионирование, скоупинг) · `docs/RUNBOOK.md` (деплой, бэкап/восстановление,
ротация ключей, разбор тревог, чеклист ввода в эксплуатацию) · раздел «модель угроз» ·
Biome (линт+формат) с включением по каталогам, прогон в CI.

---

## Сводный график

| Недели | Что |
|---|---|
| 1 | Дефекты 1–8, git, тесты движка подсчёта |
| 2 | Интеграционные тесты, CI, Docker+compose |
| 3 | Аутентификация 0.4, транспорт 0.5, защита данных 0.6 |
| 4–5 | Приглашения + киоск-режим |
| 6 | i18n оболочки + коды ошибок |
| 7 | Конструктор: разбиение + массовая вставка; E2E-смоук |
| 8 | Пагинация/производительность, импорт методик |
| 9–10 | Уведомления (email/push/эскалация), заключения+PDF |
| 11–12 | Офлайн мобилки |
| 13 | RCI, safety-план, «моя динамика» |
| фоном | RLS поэтапно, сверка ключей, новые методики, мелочи пачками |

## Проверка на каждом этапе

- `bun test` зелёный во всех пакетах, typecheck трёх приложений, E2E-смоук.
- Ручной сценарий на сиде: логин каждой ролью → групповой админ не видит чужих
  пациентов; батарея по порядку и в обход (отказ); тревога → письмо в mailhog;
  киоск-сеанс на троих; офлайн-сдача в авиарежиме с последующим синком.
- `bun run audit:verify` подтверждает цепочку журнала после серии действий.
- Восстановление бэкапа на чистую БД + смоук-логин.
