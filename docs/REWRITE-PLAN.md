# План переделки под макеты Polypsy

Документ отвечает на один вопрос: что нужно сделать, чтобы система выглядела и
работала так, как в макете `Polypsy` (Figma, 54 экрана, комментарии
трёхлетней давности).

Он написан после сплошной сверки, а не по впечатлению. Порядок такой: сначала
факты, потом развилка, которую нельзя обойти, потом сам план.

---

## 1. Как получены числа

Две независимые работы, 116 читателей, ничего не додумано:

**Опись построенного** — восемь читателей по областям (модель данных, маршруты
API, экраны, движок подсчёта, доступ, контур поликлиники, защиты, выкатка) плюс
критик полноты, который пересчитал заявленное и нашёл пропущенное.

**Чтение макета** — экспорт доски 20017×14545 нарезан по кадрам, каждый кадр
прочитан отдельно и дословно, затем сверен с кодом: где этот экран в системе,
чем отличается, чего не хватает на сервере, чем рискуем.

Что удалось поймать критиком и стоит знать до планирования:

- `ROADMAP.md` держит закрытыми пять возможностей, которых в коде нет:
  маршруты помощи, план лечения и цели, консилиум, мульти-информант, кризисный
  режим. Их таблицы удалены миграцией 0077. **Планировать по документу, который
  расходится с кодом на пять пунктов, нельзя** — это первое, что придётся
  привести в порядок.
- Мобильное приложение — не «два экрана», а **9 220 строк** и второй клиент
  сопоставимого веса: кабинет пациента плюс срез консоли специалиста (обход по
  палатам, разбор тревог, аналитика методики). При этом **выкатить его нечем**:
  нет `eas.json`, нет идентификаторов приложения, адрес сервера — `localhost`.
- Реестр маршрутов `ROUTE_DOCS` описывает не всю защиту: 15 маршрутов
  проверяют право внутри обработчика, и одно из них (`messages.write`) в
  реестре не значится вовсе.

---

## 2. Что показала сверка

### Числом

| | |
|---|---|
| Экранов в макете | 54 (прочитано 53, один служебный) |
| Совпало один в один | **0** |
| Та же задача решена иначе | **51** |
| Нет вовсе | **2** (организации) |

По объёму работы: 8 экранов — правка вёрстки, 19 — новый экран на готовых
данных, 22 — нужны новые данные и маршруты, 4 — меняют модель данных всей
системы.

### По разделам макета

| Раздел | Экранов | Дней-эквивалентов | Почему столько |
|---|---:|---:|---|
| Организации | 4 | 50 | Сущности нет вовсе, и это было решением |
| Статистика | 7 | 32 | Именованные статистические модели с конструктором фильтров |
| Тесты | 13 | 32 | Каталог, папки, конструктор простого и сложного теста |
| Администраторы | 6 | 25 | Другая ролевая модель |
| Врачи | 8 | 22 | Карточка врача как раздел, а не строка в учётках |
| Группы | 4 | 17 | Групп пациентов в системе нет |
| Сообщения | 3 | 15 | Рассылки вместо переписки один на один |
| Аналитика | 3 | 7,5 | Сохраняемые аналитические модели |
| Пациенты | 2 | 7 | Вкладки-группы, другой состав карточки |
| Публичная часть | 1 | 2 | Лендинга нет |
| Заключения | 1 | 2 | Есть, но другой формы |
| **Итого** | **53** | **212** | |

«День-эквивалент» — работа одного разработчика. 212 дней-эквивалентов — это
примерно десять месяцев для одного человека. С моим участием быстрее, но
порядок величины именно такой, и его стоит принять до, а не после.

---

## 3. Развилка, которую нельзя обойти

**Верхнее меню макета: Пацієнти · Групи · Тести · Аналітика · Статистика ·
Повідомлення.** Оно стоит на 32 экранах из 53 и, по словам самих читателей
макета, «задаёт модель данных системы».

**В этом меню нет ничего из клинического контура, который построен:** нет
расписания, нет записи на приём, нет экрана дня, нет обращений, нет
направлений, нет очереди работы, нет диспансерного учёта. Это не пропуск в
макете — это другой продукт:

| | Макет | Построено |
|---|---|---|
| Что это | Платформа психодиагностики с организациями, группами и статистикой | Рабочее место психологического отдела поликлиники |
| Единица работы | Группа пациентов и статистическая модель | Приём, обращение, случай риска |
| Учреждения | Организации внутри одной системы, публичный каталог | Отдельная база и экземпляр на каждое |
| Кому | Суперадмин платформы → админ организации → врач → пациент | Суперадмин → заведующий → специалист → пациент |

Сделать «1 в 1» буквально — значит **убрать с глаз 40 маршрутов и 11 таблиц
работающего клинического контура**, потому что в меню макета для них места
нет.

Отсюда три честных прочтения «1 в 1». Выбрать надо одно, и это решение
пользователя, а не моё.

### А. Макет — это вся система

Переписываем под макет целиком, клинический контур сворачиваем.

- **Цена:** 212 дней-эквивалентов плюс снятие того, что уже работает в бою.
- **Когда верно:** если поликлинический контур строился мимо задачи и
  психологам он не нужен.
- **Риск:** в бою лежат зашифрованные записи пациентов, привязанные к приёмам
  и обращениям. Снятие контура — это решение об их судьбе, а не о вёрстке.

### Б. Макет — это вид, а не устройство *(рекомендую)*

Берём из макета всё, что видно: верхнее меню, экраны, разбивку на разделы,
визуальный язык, конструктор теста, группы, статистические модели, рассылки.
Под ними оставляем построенный движок и клинический контур — он получает своё
место в меню, которого в макете нет, потому что в макете его и не было.

- **Цена:** 162 дня-эквивалента (212 минус организации).
- **Что даёт:** макет исполняется по экранам; ничего работающего не теряется;
  движок подсчёта, психометрика, шифрование, журнал и RLS остаются нетронутыми.
- **Чего не даёт:** буквальности в одном месте — в меню будет седьмой пункт.

### В. Макет — это раздел

Диагностическая часть делается по макету, клиническая остаётся как второй
раздел с собственным входом.

- **Цена:** та же, что у Б, плюс работа по разделению навигации.
- **Когда верно:** если у системы два разных читателя — исследователь и
  клиницист — и смешивать их экраны вредно.

---

## 4. План по волнам

Порядок выбран так, чтобы ценность приходила раньше цены, а необратимое шло
последним. Волны 1–3 меняют то, что видно сразу; волны 7–9 — самые дорогие;
организации отделены намеренно.

### Волна 0. Решения и уборка — до первой строки кода

1. **Ответить на развилку** (А / Б / В). Без этого всё остальное — догадка.
2. **Решить судьбу организаций** — см. раздел 5.
3. **Решить судьбу мобильного приложения**: довести до выкатки, переделать под
   макет (макетов телефона нет вовсе) или заморозить явно.
4. **Привести `ROADMAP.md` в соответствие с кодом** — пять возможностей
   числятся сделанными, а удалены.
5. **Убрать мусор из дерева**: `data/recordings` (393 файла), устаревшая копия
   репозитория в `.claude/worktrees/`.

### Волна 1. Визуальный язык и каркас · ~8 дней-экв

Верхнее меню вместо боковой рельсы, знак POLSY, типографика и палитра макета,
подвал с контактами. Каркас трогает все 43 экрана сразу, поэтому идёт первым:
делать его после экранов — значит переделывать экраны дважды.

Сторож: скриншотные эталоны пересняты в обеих системах; проверка контраста и
доступности не должна просесть.

### Волна 2. Группы пациентов · ~17 дней-экв

Самая частая новая сущность в макете (36 упоминаний нехватки на сервере).
Группа со своим описанием, составом, вкладками в списке пациентов и —
главное — **назначением методики на всю группу**, которого сейчас нет:
назначение адресуется одному человеку.

Новое на сервере: таблицы группы и состава, маршруты, назначение на группу
поверх существующего `grantAccess`.

Осторожно: план системы прямо запрещал массовое назначение («превращает
методику в рассылку и обесценивает её»). Это решение придётся пересмотреть
осознанно — и записать, почему.

### Волна 3. Тесты: каталог и конструктор · ~32 дня-экв

Папки, вкладки «Опубліковані / Неопубліковані», конструктор с вкладками
«Конкретний тест / Комплексний тест», таблица баллов, таблица результатов с
формулой.

**Движок трогать не нужно** — и это главная хорошая новость всей сверки:

| В макете | В коде |
|---|---|
| Шкала → Так/Ні/Нінаю → номера вопросов → баллы | `ScaleItem { matchKey, weight }` |
| «Шкала 2 · Ні · × 0.5» | `ScaleCorrection { coefficient }` |
| «від 0 до 10 → текст» | `scale_bands` |
| Мова: укр / рус / eng | двуязычное содержимое со сторожем полноты |

Работа здесь — интерфейс к тому, что уже считается. Золотые тесты подсчёта
защищают от порчи: они ловят изменение балла на единицу.

### Волна 4. Прохождение и разбор · включено в волну 3

Экраны «Processing Simple/Complex Test», «Completed Test», «doctor's opinion».

### Волна 5. Заключения · ~2 дня-экв

Есть, но другой формы. Дешевле всех остальных.

### Волна 6. Аналитика: модели · ~7,5 дня-экв

Сохраняемые аналитические модели как справочник, а не как часть теста.

### Волна 7. Статистика: модели и фильтры · ~32 дня-экв

Именованная статистическая модель с конструктором фильтров по демографии
пациента (дата, возраст от-до, пол, населённый пункт) и двумя уровнями
детализации: по результату теста и по отдельному вопросу с процентами
ответов.

**Здесь придётся столкнуться с порогом малых чисел.** В системе стоит
k-анонимность: ячейки меньше пяти подавляются, и подавляются дополняюще,
чтобы скрытое не восстанавливалось вычитанием. Конструктор произвольных
фильтров — это машина для нарезки выборки до одного человека. Либо фильтры
подчиняются порогу (и тогда часть срезов макета будет пустой), либо порог
снимается — но тогда систему нельзя называть обезличенной.

Это второе место после организаций, где «1 в 1» упирается не в работу, а в
решение.

### Волна 8. Сообщения · ~15 дней-экв

В макете «Повідомлення» — раздел с рассылками: тема, текст, поиск, создание.
В системе — переписка пациента со специалистом один на один, с явно
названными границами («ответ в рабочее время, это не экстренная связь»).

Это разные вещи. Рассылка — новое; переписку при этом терять нельзя.

### Волна 9. Люди: врачи и администраторы · ~47 дней-экв

Карточка врача как раздел, списки администраторов, схемы сайта по ролям,
добавление врача и администратора. Ролевая модель макета
(суперадмин платформы → админ организации → врач) ложится на построенную
(класс учётки + роли-шаблоны + права + личные исключения) не один в один:
у построенной прав 29 и своя лестница должностей.

### Волна 10. Публичная часть · ~2 дня-экв

Лендинг «Про кампанію», FAQ, «Для преси», подвал с контактами и соцсетями.
Дёшево, если тексты идут через словарь; дорого, если их надо править из
кабинета.

### Волна 11. Организации · ~50 дней-экв, только по решению

См. раздел 5.

---

## 5. Организации: единственное, что нельзя сделать «как нарисовано»

В макете организация — запись в системе: у неё есть форма создания (название,
телефон, email, страна, город, тип), карточка со списками администраторов и
врачей, список организаций и **публичный каталог с поиском**.

В построенной системе организаций нет вовсе: `organisation_id` / `tenant_id`
не встречается ни разу в 68 таблицах. Это записанное решение — каждое
учреждение получает свою базу и свой экземпляр.

Три пути:

**5.1. Оставить как есть.** Организация = экземпляр. Экраны организаций из
макета не делаются, публичный каталог не делается. Цена — ноль, расхождение с
макетом — четыре экрана.

**5.2. Добавить измерение арендатора.** Колонка организации в 68 таблицах, 64
политики RLS переписываются, каждый запрос получает новое условие, миграция
боевых данных в «организацию по умолчанию».

Это не «ещё один экран» — это операция на несущей конструкции системы,
которая хранит зашифрованные медицинские записи. Ошибка в одной политике —
это врач одной организации, видящий пациентов другой.

**5.3. Реестр без арендатора.** Организации заводятся как справочник и
публичный каталог, но данные по-прежнему разделены экземплярами: каталог
знает об организациях, экземпляр — только о себе.

Компромисс: экраны макета появляются, несущая конструкция не трогается. Это
то, что я бы делал, если «организации» нужны ради каталога и заявок, а не
ради общей базы.

---

## 6. Риски и порядок, который бережёт данные

1. **Сначала вид, потом устройство.** Волны 1 и 3 (каркас и конструктор) ничем
   не рискуют: они не трогают ни модель данных, ни подсчёт. Начинать с них —
   значит показать результат раньше, чем потратить необратимое.
2. **Движок подсчёта не трогаем.** Четыре выверенные методики и золотые тесты —
   единственное в системе, чью правильность подтверждает не код, а психометрия.
3. **Ничего не удаляем, пока не решена развилка.** Снятие клинического контура
   необратимо для данных, которые к нему привязаны.
4. **Каждая новая таблица — с политикой RLS в той же миграции.** Сейчас 64
   политики держат изоляцию; таблица без политики — дыра, которую видно только
   проверкой покрытия.
5. **Строки интерфейса — только через словарь.** `uiStrings.ts` (2 907 строк) —
   единственный словарь и для консоли, и для мобильного, со сторожем на 728
   строк регулярок. Любая переделка экранов проходит через него.

---

## 7. Что нужно от заказчика до старта

1. **Развилка А / Б / В** — какой смысл вкладывается в «1 в 1».
2. **Организации** — 5.1, 5.2 или 5.3.
3. **Статистика и порог малых чисел** — подчиняются фильтры порогу или нет.
4. **Наработки психологов** — это сам макет или отдельный материал (методики,
   ключи, нормы)? Если второе, то большая часть работы не в переделке
   интерфейса, а в заведении их методик в движок, который уже есть и уже умеет
   всё, что нарисовано в конструкторе.
5. **Мобильное приложение** — доводить, переделывать или заморозить.

Пятый вопрос — самый недооценённый: приложение написано полностью, но выкатить
его нечем, и макетов под телефон нет ни одного.

---

## 8. Решение принято: делаем 1 в 1

Заказчик выбрал прочтение **А** — макет как вся система. Возражение про
клинический контур было высказано до решения и снимается: работаем по макету.

Из этого следуют три правила работы, которые дороже любого экрана.

**Ничего не удаляем ради вида.** «1 в 1» касается того, что человек видит.
Шесть пунктов меню — ровно шесть, как нарисовано; всё остальное уходит за
бургер, который на макете тоже нарисован. Маршруты, таблицы и зашифрованные
записи остаются на месте. Снятие клинического контура — отдельный
необратимый шаг, и он делается по отдельной команде, а не попутно с вёрсткой.

**Сначала каркас, потом экраны.** Каркас (палитра, шапка, базовые компоненты)
входит во все 43 экрана. Сделать его вторым — значит переделать каждый экран
дважды.

**Доступность не размениваем на буквальность.** В макете три места, которые
буквально повторить нельзя без вреда: подпись поля только плейсхолдером
(диктор остаётся без имени поля), «плюс» и стрелки глифами по 5×7 пикселей
(провал по размеру цели нажатия), активная вкладка отличается только цветом.
Вид сохраняем, а диктору и клавиатуре добавляем то, чего на картинке не
видно: `aria-label`, область нажатия, `aria-current`. Это не отступление от
«1 в 1» — на экране всё выглядит как нарисовано.

### Что замерено с макета

Палитра снята пипеткой, не подобрана:

| Цвет | Где |
|---|---|
| `#663399` | меню, заголовки, имена, вкладки, иконки, текст кнопок |
| `#ab8fcc` | текущий раздел меню — бледнее остальных |
| `#f0ecff` | полоса шапки, заливка кнопок и полей фильтра |
| `#666666` | второстепенный текст и рамка полей ввода |
| `#cccccc` | линии, разделители, рамка селектов |
| `#999999` | рамка выпадающих панелей, ползунок прокрутки |

Геометрия: шапка 100px, содержимое 1200px по центру, поле ввода 36px, радиус
4–5px, шаг строк формы 51px, шаг строк списка 58px, зазор пунктов меню 46px,
зазор вкладок 53px.

Типографика: заголовок раздела 24/700, меню 20/700, вкладки 18/700, кнопка
22/700, плейсхолдер 17/700, имя в списке 13/700, мета 13/400, микроподписи
10/400. Всё, кроме серого текста, — полужирное. Шрифт — геометрический
гротеск с одноэтажной «а».

### Волна 1 — сделана

Три непересекающихся файла, три исполнителя параллельно:

1. `styles/tokens.css` — палитра POLSY, светлая тема по умолчанию.
2. `shell/Topbar.tsx` + `App.tsx` — верхнее меню вместо рельсы, остальные
   разделы за бургером.
3. `ui/primitives.tsx` + `ui/layout.tsx` — кнопка, поле, вкладки, панель над
   списком, строка списка по образцам.

Словарь пополнен заранее одним автором, чтобы исполнители не подрались за
`uiStrings.ts`: заведены `top.patients`, `top.groups`, `top.tests`,
`top.analytics`, `top.statistics`, `top.messages`, `top.more`.

Отдельно отмечено: макет называет методики «тестами». Слово на экране взято с
макета, прежний ключ `nav.surveys` остаётся там, где текст не с макета.

### Что легло в волне 1 и где вид отступил от буквы

Каркас: полоса шапки 100px `#f0ecff`, знак POLSY, шесть пунктов с шагом 46px,
текущий бледнее, «Укр» и бургер по краю колонки 1200px; всё, что в шесть
пунктов не вошло, — за бургером с ловушкой фокуса. Светлая тема — по
умолчанию: в токенах, в трёх местах кода и миграцией 0079, которая убирает
из профилей «dark», записанный туда без выбора человека.

Три отступления от буквы макета — все ради читаемости, и все в коде с числами:

| Макет | Сделано | Почему |
|---|---|---|
| Текущий раздел `#ab8fcc` | `#7a4ea6` | 2,79:1 на полосе — вдвое ниже нормы; 5,23:1 у замены |
| Неактивная вкладка `#b299cc` | `--primary-dim` | 2,52:1 на белом; активная от неактивной по-прежнему отличается светлотой |
| Серый текст `#666666` | `#595959` | 4,96:1 на полосе шапки при правиле файла 5,0 |

И одно решение, которого макет не задаёт: умолчание кнопки — компактное
(36px, 15/700), крупная кнопка формы (45px, 22/700) — по явному указанию.
Макет рисует только кнопки форм; строчных действий в нём нет, и правила для
них нет. Первая редакция сделала наоборот и раздула сто восемьдесят кнопок
консоли до заголовков.

Подпись поля живёт внутри как плейсхолдер, как на макете, — но диктор
получает имя: скрытая подпись внутри `Field`, а у поля с `id` подпись
через `<label for>` не перебивается. Ровно на этом упал сценарий ввода
бланка, и это был настоящий дефект, а не тест.

Скриншотные эталоны сняты в светлой теме на обеих системах: сторожить надо
то, что обещано, а обещан макет.


### Волна 2 (сервер) и папки каталога — сделаны, v1.5.1

Группы пациентов заведены как `patient_groups` — имя нарочно не похоже на
`survey_groups`, группы методик и единицу разграничения доступа. Назначение
методики на всю группу разворачивается в поимённые выдачи через тот же
`grantAccess`: у каждого свой срок и число попыток, в карте видно «через
группу». Запрет плана на массовое назначение снят по решению заказчика; что
из этого следует, записано в докблоке назначения. Запрет на повторяющееся
назначение по расписанию остаётся.

Папки каталога методик принадлежат группе методик, а не человеку: личные
папки дали бы коллегам разные каталоги одной методики. Составной внешний
ключ `(folder_id, group_id)` заставляет базу держать «методика лежит в папке
своей группы». Постраничность `GET /api/surveys` — offset: макету нужны
«сторінка 1 з 10» и шаг назад, курсор знает только «дальше».

Политики RLS — в тех же миграциях, что таблицы. Двадцать шесть мутаций,
каждая уронила свой тест с названным виновником. Ловушка учтена: тестовый
пользователь базы обходит RLS, поэтому политики проверяются текстом из
`pg_policies`, предикаты — прямым вызовом.

### Тема: почему потребовались миграция и новый ключ

Прежний код сохранял тему при каждом монтировании — и в профиль на сервере,
и в браузер — включая самый первый раз, когда тема была умолчанием, а не
выбором. Стоило умолчанию стать светлым, как оно перестало работать для всех,
кроме новых учёток: сохранённое «dark», которого никто не выбирал, перебивало
новое. Профиль чистит миграция 0079; до браузера серверу не дотянуться, поэтому
ключ хранилища сменён на `quizzy.theme.v2` — старое авто-значение не читается,
новое пишет только переключатель. Проверено на стенде.

### Волна 3 — экраны «Тести»: как шла

Четыре экрана делались параллельно в отдельных рабочих копиях, каждый — по
циклу «сборка → враждебная проверка → правка». Проверяющему ставилась
задача опровергнуть, что сделано один в один и без вреда: он сверял код с
кадром по пунктам и запускал проверки сам. Все три готовых экрана
(просмотр пройденного теста, заключение, каталог) получили замечания, и все
замечания исправлены до слияния.

Что нашлось проверкой и стоит запомнить:

- Правило наследия `h3 { text-transform: uppercase }` и `h1, h2 { font-family:
  display }` в `legacy.css` душило заголовки любого нового экрана — вид «как
  на макете» получался только вопреки ему. Правило сужено до бесклассовых
  заголовков наследия; все h1/h2 консоли теперь в основной гарнитуре, как
  на макете. Скриншотные эталоны после этого пересняты.
- Два экрана независимо решили одно и то же по-разному: просмотр встраивал
  редактор заключения на месте, заключение стало отдельной страницей с
  протоколом ответов. Оставлена страница — у неё есть адрес, а протокол там
  виден так же; кнопка просмотра ведёт на неё.
- Сторож «ссылка на прохождение по форме маршрута» не знал новой страницы
  заключения и счёл её зовом в пустоту. Сторож научен читать объявленные
  маршруты, и проверен снятием: без маршрута в `App.tsx` он падает.
- Меню каталога объявляли `role="menu"`, но ловушка фокуса знала только Tab;
  стрелки, Home и End добавлены, фокус встаёт на первый пункт.

Каталог: папки с крошками и датой, вкладки «Опубліковані / Неопубліковані»,
поиск, «+» с меню, постраничность «елементів на сторінці · сторінка N з M».
Третья вкладка «Зняті» — отступление от макета с причиной: без неё у снятой
методики нет дороги назад.

Конструктор слит последним: один свиток по восьми кадрам — вкладки
«Конкретний / Комплексний тест», название и описание, вопросы аккордеоном,
ответы чипами, карточка шкалы с «Відповідність» (ответ → номера вопросов →
баллы), «розрахунок балу» чипами поправок и «Результати» диапазонами.
Проверка нашла и исправила: второй значок «призначити групі» был выброшен по
ложному утверждению «сервер этого не умеет» — умеет с v1.5.1; шкала без ключа
не давала собрать ключ в форме; смена группы не сбрасывала папку из адреса.

Чего модель подсчёта НЕ выражает, и что макет рисует — записано, не выдумано:

- слагаемое формулы «шкала × конкретный ответ» (`[Шкала 2][Ні] × 0.5`) —
  поправка берёт балл всей шкалы, а не ряда ответа;
- балл на паре «шкала + ответ» одним числом — хранится на паре «шкала +
  вопрос», ряд правит вес всех своих пунктов;
- общий набор ответов теста — есть только в черновике, в базе варианты у
  вопроса;
- ~~ключевой лист знает «так/ні», третий ответ («Нінаю») выпадает из
  печати~~ — закрыто в v1.6.1: колонка на каждый код ответа из вариантов;
- третий язык содержимого (English) — отложен решением волны 22. Английский
  как язык ИНТЕРФЕЙСА заведён в волне 9 (решение заказчика 2026-09-26):
  оболочка, отказы сервера и уведомления — на трёх языках, а текст методик
  остаётся на двух и при английском интерфейсе показывается по-украински
  (`CONTENT_LANGS` в `packages/shared/src/types.ts`).

Это кандидаты на отдельное решение: расширять движок ради буквы макета или
оставить как есть. Движок в волне 3 не тронут.

### Довесок сервера к волне 3 — v1.6.1

Экраны волны 3 рисовались на сервере v1.5.1 и дорисовали сами то, чего он
не отдавал: каталог фильтровал «снятые» на клиенте, просмотр брал веса и
лестницу из действующей версии методики (а человек мог проходить прежнюю),
ключевой лист печатался на два ответа. Сервер догнал:

- каталог: `?archived=only`, `?status=` списком, поиск по поддереву папки
  рекурсивным CTE — корень поддерева не имеет, поиск по всему каталогу идёт
  без `?folder`;
- прохождение отдаёт вес выбранного варианта (персоналу; обследуемому —
  `null`, иначе вес подсказывает «правильный» ответ) и всю лестницу полос той
  версии, которую проходили, с пометкой попавшей;
- методика по номеру версии `?version=N` — после проверки прав, только среди
  версий этой методики;
- ключевой лист общий для любого набора кодов ответов.

Двенадцать мутаций, каждая уронила свой тест. Заготовки тестов консоли
дополнены новыми обязательными полями типа.

Эталоны экранов «Тести» сняты: каталог, конструктор (новый и правка),
просмотр, заключение — darwin и linux по очереди. Идентификаторы для них
берутся из записанного списка, а не живым запросом: посев случайный и
пересоздаётся каждый прогон. Экраны со свитком снимаются целиком — окно
вытягивается на скрытую высоту, иначе лестница и редактор оставались за
краем.

Эталоны сразу нашли два расхождения, оба закрыты:

- `getSurveyForResponse` читала методику без языка — всегда по-украински, —
  а описание экран брал другим запросом с языком консоли; под русской
  консолью выходил украинский заголовок над русским описанием. Язык теперь
  берётся у читателя;
- пункт «порядок» просмотр рисовал списком вариантов с нулями, а заключение —
  цепочкой «→». Просмотр выровнен по заключению; варианты всё же
  перебираются — по ним ловится чужая версия.

### Волна 4 — «Групи», «Люди», «Аналітика» — сделана, v1.7.0

Три сборщика в своих копиях, ревью каждого, правка по находкам, слияние.

**Группы пациентов** (f05/f10/f20, сервер v1.5.1): `/patient-groups` —
список с поиском, «+», страницами и сердцем «обрана»; карточка группы —
описание, «Тести Групи» с назначением группе, «Пацієнти Групи» сеткой 3×N
с выбором и «Прибрати з групи»; `/patients` переписан по кадру — вкладки
«Усі · группы», сетка карточек, «N вибрано» с действиями «Додати до
групи» и «Призначити тест». Пункт «Групи» верхней полосы теперь ведёт на
группы пациентов; группы методик остались в бургере под именем «Групи
методик» — адрес `/groups` не менялся, закладки живы. Ревью вернуло
сохранённые виды на `/patients`, которые ушли вместе с таблицей.

**Люди** (f32–f36, f41–f44, f48–f51 — 17 кадров в четыре экрана): `/staff`
и `/admins` — сетки 3 колонки; карточка с вкладками «Профіль · Пацієнти ·
Групи», шестерёнка с меню; заведение врача и администратора. Ролевая
модель макета легла на построенную: лікар = класс `admin`, администратор =
суперадмин либо ступень лестницы выше специалиста. Ревью нашло брак:
справочник бил в закрытый `GET /api/users` и по 403 откатывался — каждое
открытие писало в журнал доступа отказ, и штатная работа выглядела там как
попытки взлома. Теперь маршрут выбирается по праву `users.manage` из
карточки прав, которую консоль читает вместе с `/me`.

**Аналитика** (f08/f15/f27): «аналитическая модель» — это правило
поддержки решений (`decision_rules`), новых сущностей не заведено;
перечень с поиском и страницами, конструктор с параметрами, действиями и
меню шестерёнки. Чего модель правил не выражает — записано ниже. Ревью:
селект теста не показывал методику, которой нет в списке (снятую или вне
зоны) — теперь она стоит опцией-сиротой с пометкой.

**Слияние.** Три ветки независимо вынесли из каталога одни и те же
помощники — пейджер, меню, глифы — тремя разными модулями, причём
`ui/Pager.tsx` и `ui/pager.tsx` отличались только регистром: на macOS
такое дерево не выкладывается. Сведено к одному набору строчными:
`ui/pager.tsx` + `ui/paging.ts`, `ui/glyphs.tsx` (объединение пяти
глифов; `GearGlyph` и `IconGear` оказались одним контуром), `ui/menu.tsx`
(ядро `MenuButton` со стрелками, Home/End, Esc и ловушкой фокуса;
`ActionMenu` — обёртка над ним). Урок для следующих волн: общее выносит
один сборщик, остальным — дублировать локально до слияния.

Пробелы сервера, которые экраны обошли (кандидаты в серверную волну 5):
единый список пациентов зоны с группой, поиском и страницами; пол и год у
участника группы, описание у теста группы; пакетное добавление и удаление
участников; признак «обрана» на сервере (сейчас localStorage); карточка
сотрудника по id, правка чужой анкеты и сброс пароля; список пациентов
врача по закреплению; у правил — «Або» и вложенные группы условий, условие
по ответу на вопрос и по времени реакции, `?q`/страницы, чтение и удаление
по id, сухой прогон по прошлым прохождениям.

### v1.7.1 — символы интерфейса своими шрифтами

Деплой v1.7.0 упал на CI без единой правки кода: смена образа раннера
GitHub сменила системный запасной шрифт, которым рисовались «←» крошек,
«→» цепочки порядка и «▸» раскрывающихся разделов — все три вне
`unicode-range` локальных подмножеств. На macOS то же место проходило по
допуску. Стрелки и «≈», «≥» — подмножество Onest в 1,6 КБ
(`Onest-symbols.woff2`), «▸» — свой SVG в `ui/glyphs.tsx`. Сторож
`fontCoverage.test.ts` читает диапазоны из `fonts.css` и не пускает новые
символы мимо них; остаток (✕ ⚠ ★ ↶ ↷ ↵ ⌘ ⧉ └ ─ α) перечислен поимённо и
добивается следующим коммитом. Это и про «1 в 1»: читатель на Linux
видел другую стрелку, чем на macOS, — не тот интерфейс, что нарисован.

**Известный пробел мобильного клиента (не правится: apps/mobile
заморожено).** Выход из учётной записи серверную сессию не отзывает:
`AuthContext.logout` чистит только локальное хранилище
(`tokenStorage.clear()`), а `POST /api/auth/logout` не зовёт — в отличие от
веба (`apps/web/src/api.ts`). Refresh-токен остаётся действительным до
истечения срока, и снятая с телефона пара токенов продолжает открывать
карты после того, как человек нажал «выйти». Чинится одной строкой при
разморозке мобильного: вызвать `/api/auth/logout` с refresh-токеном до
очистки хранилища.

### Волна 5 (сервер) — «Статистика»: пресеты фильтров и модели

Кадры f06, f07, f14, f21, f25, f26, f28. Сервер под экраны следующей
волны; веб не тронут.

Что заведено: `filter_presets` (владелец, название, строки-критерии:
дата от/до, возраст от–до, пол, населённый пункт, группа пациентов,
пациент) и `stat_models` (название, короткое описание, колонки одним
jsonb: пресет либо свои фильтры, методика с версией, показатели — полосы
шкал и варианты ответа с пометкой «ВШР»). Маршруты `/api/filter-presets`
и `/api/stat-models` (CRUD, `?q`/`?limit`/`?offset` → `{items,total}`),
расчёт `POST /api/stat-models/:id/run` («Оновити») и превью без
сохранения `POST /api/stat-models/run` («Порівняти»). Новое право
`statistics.read` — заведующему и главному врачу, специалисту нет.
У пациента появился `users.locality` открытым текстом (шифрованное поле
нельзя сравнить в SQL); правится в `PATCH /api/auth/me` и при
регистрации; посев раздаёт четыре города.

Прочтения кадров, которые из макета буквально не следуют:

- **«ВШР»** прочитано как «високий ступінь ризику» — пометка на
  показателе (полоса или вариант ответа), попадание в который аналитик
  считает признаком риска. Расчёт отдаёт по колонке долю людей хотя бы с
  одним таким попаданием (`highRisk`), подавляя её с обоих краёв порога.
  На макете аббревиатура не расшифрована; альтернатива — «вес показателя»
  — отвергнута: у макета нет поля для числа, только кнопка-тег.
- «Варіант результату» — полоса шкалы версии методики, «Відсоток
  відповідей» — доля респондентов колонки в ней; «Текст питання / Текст
  відповіді» — вопрос с вариантами (single, yesno, multiple; матрица и
  ранжирование в строку макета не ложатся — отказ кодом).
- Нижние строки f28 «Назва моделі / Назва повідомлення» с «ВщВМ» —
  ссылки на другие модели и рассылки; в этой волне не заведены:
  сообщения делает соседний пакет, а смысл «ВщВМ» из кадра не
  определяется.
- Респондент — человек, по последнему сданному прохождению той версии,
  что записана в колонке; анонимные прохождения не считаются.

**Порог малых чисел** (второе место после организаций, где «1 в 1»
упиралось в решение): фильтры подчинены порогу. Ячейка меньше пяти —
`{suppressed: true}`, не ноль и не пропуск; доли — от показанного
основания; колонка меньше пяти подавляется целиком. Одного порога мало:
ячейки отчёта — система уравнений, и читатель решает её, а не сверяет
числа. Защита стоит на трёх этажах:

- **группа** — полосы шкалы с остатком, варианты вопроса с остатком:
  показываются целиком или прячутся целиком. Под порогом хоть одна ячейка
  (у множеств — с обоих краёв, плюс пересчёт «сумма ячеек минус
  объединение») — скрыта вся группа: пока показана хоть одна ячейка и
  скрыта хоть одна, у системы остаётся ровно одно неизвестное.
- **колонка** — «ВШР» стоит поверх групп, объединяя помеченные множества;
  прячется над скрытой группой с пометкой, под двусторонним порогом и
  когда помеченные множества пересекаются горсткой людей.
- **модель** — две колонки, различающиеся горсткой людей, описывают эту
  горстку разностью своих чисел; закрывается колонка с малой разностью,
  причина приходит в `suppressedReason: "overlap"`. Равные составы —
  дубль, а не утечка.

Дополняющее подавление (`suppressedKeys`) осталось только у срезов, где
разбиение настоящее, а пара доказуема: ноль в пару не годится, и отрезок
допустимых значений скрытой ячейки обязан содержать хотя бы два числа —
иначе прячется всё разбиение. Проверено девятью мутациями: проверка
перебирает допустимые значения скрытых ячеек и областей диаграммы и
падает, называя вопрос, полосу или область («Думки без Шум — ровно
четверо»). На стенде модель «Сон: Київ проти Львова» показывает это
вживую: Київ — шестеро, колонка открыта; Львів — трое, подавлена целиком.

## 9. Сверка «один в один»: как она идёт и что вскрыла

После волны 6 начата построчная сверка каждого готового экрана с его
кадром: замер по пикселям → правка в своей копии → скептик перемеряет
заново. Скептик обязателен: в трёх разделах из трёх он находил и
расхождения, которые правщик считал закрытыми, и регрессы, внесённые
самой правкой.

**Кадры пришлось нарезать заново.** Временный каталог, где они лежали,
был вычищен вместе с описаниями и пакетами волн; экспорт доски уцелел.
Нарезка восстановлена по прозрачности фона (кадры — непрозрачные
острова на пустом листе), 53 кадра описаны заново и лежат теперь в
постоянном месте `~/Documents/polypsy-frames` вместе с описью и своим
окружением для замеров. Нумерация новая и со старой, упомянутой выше в
этом плане, не совпадает — старые номера остаются историей.

**Что вскрыла опись кадров, чего в плане не было:**

- **шапок в макете четыре, а не одна**: у специалиста шесть разделов, у
  администратора семь (седьмой «Лікарі»), у административной консоли —
  один пункт «Лікарі» по центру, у суперадминской — один «Адміністратори»;
- **«Заключення» нарисовано отдельным конструктором** — список
  применённых аналитических моделей с пометками «Відповідає умовам»,
  список тестов с набранным баллом, редактор с гарнитурой и начертанием,
  «Зберегти як PDF / Надіслати поштою»; у нас это секция карточки;
- **фильтры статистики — отдельная сущность** с пресетами и экраном
  управления, а не набор полей внутри модели;
- **сравнение двух выборок рядом** с диаграммой по группам.

**Регрессы, пойманные скептиками (и закрытые):** действия строки каталога
держались на наведении и пропадали на планшете; подсказка у выключенной
полосы форматирования не всплывала, потому что у отключённых кнопок
браузер не доставляет события мыши; выпадающие списки лишились каретки и
стали неотличимы от полей; правка меню одного раздела задела все меню
приложения; вид заведённого параметра стало нечем сменить; общий верхний
отступ увёл чужие экраны; разделы своей карточки стали недостижимы.

**Чего сверка достичь не может без сервера** (записано, не выдумано):
второй блок условий и «Або» в правилах, условие по конкретному ответу и
по времени реакции, связь «заключення → кілька проходжень», формат
разметки в тексте заключения, связь «адміністратор → його лікарі»,
телефон в списках (шифрован и наружу не отдаётся).

## 10. Сквозные сценарии зависели от порядка файлов

Выкатка v1.7.3–v1.7.5 не состоялась трижды: проверки в CI падали, а
локально всё было зелёным. Причина оказалась одна на три случая —
**порядок обхода каталога**: на macOS он алфавитный, на Linux
произвольный. Юнит-тесты делят одну базу, и проверка двуязычности видела
фикстуры соседей; сквозные сценарии делят посев, и до проверок
доступности успевали отработать те, что забирают случаи в работу и
сдают назначенные методики.

Доказано на самом раннере: тот же файл сценариев в одиночку проходит
целиком, а в общей очереди падает. Лечение не в закреплении порядка, а в
том, что каждая проверка заводит себе данные сама — так уже сделаны
сторож двуязычности (своя база с посевом) и очередь расшифровки (своя
запись первой в очереди).

## 11. Сверка закончена по всем существующим экранам — v1.8.1

Шесть разделов сведены с кадрами: «Тести», «Аналітика», «Люди»,
«Пацієнти/Групи/Заключення», «Повідомлення», публичная часть со входом.
Порядок один и тот же: замер по пикселям → правка в своей копии →
скептик перемеряет заново. Скептик обязателен: в каждом разделе он
находил и недоделки, и регрессы, внесённые самой правкой.

**Что дала проверка проверяющего.** Пойманные регрессы: действия строки
каталога держались на наведении и пропадали на планшете; подсказка у
выключенной полосы форматирования не всплывала (у отключённых кнопок
браузер не доставляет события мыши); выпадающие списки лишились каретки;
правка меню одного раздела задела все меню приложения; вид заведённого
параметра стало нечем сменить; общий верхний отступ увёл чужие экраны;
разделы своей карточки стали недостижимы; два маршрута оказались без
единой ссылки; в списке рассылок дата и превью ушли под aria-hidden;
площадка нажатия «Укр» сдвинула его на всех экранах; ссылка подвала
стала ниже минимальной цели нажатия.

**Цвет вкладок — закрыт замером по содержимому.** Одна сверка перевернула
активную и неактивную, выведя «активна» из бледного цвета по описи,
которая сама вывела это из цвета. Разбор: активная полная #663399,
неактивная бледная #b299cc — видно по тому, что нарисовано под вкладкой
(f22 «Конкретний тест» со «Шкала 1», f21 «Комплексний тест», f07
«Неопубліковані»). Сторож в два слоя ловит и перестановку веток, и
подмену через переименование токена.

**Где кадры спорят сами с собой** (записано, а не замолчано): подсветка
текущего пункта верхней полосы стоит на кадрах списков консоли
специалиста и отсутствует на карточках; «Лікарі» на одном кадре полным
цветом, «Адміністратори» на другом — бледным, экран тот же. Подсветка
снята: бледный тон на полосе #f0ecff даёт 2,41:1.

**Чего нельзя сделать без сервера** — второй блок условий и «Або» в
правилах, условие по ответу и по времени реакции, связь «заключення →
кілька проходжень», разметка в тексте заключения, связь «адміністратор →
його лікарі», телефон в списках.

Осталось по макету два куска, которых в системе нет вовсе: статистика
(семь кадров; сервер написан, экраны — следующая волна) и организации
(четыре кадра; решение за заказчиком — сейчас одно учреждение равно
одному экземпляру системы).


## 12. Прохождение под боевой ролью базы — v1.8.2

Найдено по логу прода, а не сюитой: отправка прохождения с критическим
пунктом падала с «new row violates row-level security policy for table
"alert_cases"», транзакция запроса откатывалась, ответы пропадали. Человек
с тревожным сигналом — ровно тот, ради кого тревоги написаны, — оставался
незамеченным, а на экране видел «не сохранилось».

**Почему сюита молчала.** Тесты ходят в базу владельцем, а владелец в
PostgreSQL политики строк обходит: полторы тысячи зелёных тестов
доказывали, что подсчёт верен, и ничего — про политики. Путь отправки
теперь прогоняется отдельным процессом под ролью без прав владельца
(`test/submissionRls.test.ts`, тот же приём, что у рассылок).

**Что было не так по существу.** Автоматика поверх отправки — случай для
дежурного, срабатывание правила, каскад назначений, закрытие батарей —
шла под ролью того, кто сдал. Таблицы этой автоматики закрыты пациенту
намеренно: ему нечего видеть в очереди разбора. Но рождает эти строки
именно его отправка. Отсюда `asSystem` (`db/context.ts`): системная роль
на время автоматики, в той же транзакции, с возвратом прежней роли в
`finally`. Отвергнуты два пути: политика «пациент вставляет случай о
себе» (случай ещё ищется и обновляется, а значит пациент должен был бы
видеть, что его разбирают как риск) и отдельный `systemContext` (другая
транзакция на другом соединении: случай мог закоммититься при откате
прохождения, а незакоммиченного прохождения он не видит).

**Вторая дыра, тихая.** Срабатывания правил поддержки решений под ролью
пациента терялись молча: движок правил ошибку глотает по замыслу («отказ
надстройки не должен ронять сдачу»), а `rule_hits` пациенту закрыта. И
групповые правила для самообращений не оценивались вовсе — политика
`decision_rules` показывает пациенту только общие. Оба пункта закрыты тем
же `asSystem`; мутация «убрать обёртку» роняет тест по срабатыванию.

**Шум, который остался.** Полоса консоли опрашивает счётчики случаев,
направлений, очереди и приёмов дня, не глядя на права: у сотрудника без
`alerts.review` или `referrals.manage` это по четыре предупреждения 403 в
минуту в логе. Не ошибка для человека, но ошибки в логе за этим шумом
искать труднее — закрыть отдельно.

## 13. Каталог методик: 19 новых по досье — v1.9.0

Каталог вырос с восьми методик до двадцати семи: PHQ-4, PHQ-8, CESD-R,
SRQ-20, GDS-15, DASS-42; PC-PTSD-5, CES, SBQ-R; MSPSS, OSSS-3, RSES,
UCLA-3, Brief COPE; CAGE, AUDIT-C, ASSIST, ASRS-6, CBI. Каждая полоса
сводится к записи досье (`docs/instruments/dossiers/`), запись — к
первоисточнику; у каждой группы свой файл золотых протоколов с примерами,
посчитанными руками. Скептик пересчитал все примеры и сверил каждую полосу.

**Что снято, а не добавлено.** У PSS-10 были полосы 0–13/14–26/27–40 — у
автора их нет («there are no score cut-offs»), и верхняя открывала случай
в очереди разбора. Сняты. DASS-42 и шкалы благополучия (RSES, UCLA-3, Brief
COPE) — без полос: пороги либо только в платном руководстве, либо их нет.
Украинский текст PHQ-9 и PHQ-8 — дословно из наказу МОЗ № 1003; GAD-7 в
наказе нет, и он остался рабочим переводом.

**Тяжесть — не вероятность.** GDS-15 «> 10 almost always depression» —
вероятность, поэтому верхняя полоса moderate, а не severe. CBI: выгорание
не риск, случай открывает только верхняя полоса (75+), иначе при датской
норме очередь заполнилась бы рабочей усталостью.

**Каталог наконец обновляется.** Установщик узнавал методику по ключу и
больше её не трогал — правки каталога доходили только до новых установок.
Теперь редакция узнаётся по отпечатку в заметке версии, отличие выкатывается
новой версией (старые прохождения остаются при своей), а методика, которую
учреждение правило в конструкторе, не перетирается и называется в отчёте.

Не заведено (нет текста или разрешения в досье): STAI, CES-D, HSCL-25,
BDI-II, BAI, HADS, GHQ-12, MADRS, IES-6, MIES, C-SSRS, WHODAS, BRS, AIS, MDQ.

## 14. Телефон пациента — как на макете (v1.9.1)

Решение заказчика 2026-09-25: «показывай, делай все как на макете». Телефон
стоит в мета-строке списка пациентов (f05), в составе группы (f14), в списке
обследованных и в плашке карточки (f13). Прежнее решение плана — номер только
отдельным действием «показать телефон» с записью в журнал — заменено этим.

Что осталось от прежней защиты: номер в базе шифрован, наружу идёт только
расшифрованный номер (шифротекст — нет, сторожится тестом); каждое чтение
списка и карточки пишется в журнал с пометкой, что номера в ответе были
(`phones: true` / `phone: true`), — на вопрос «кто видел телефоны» по-прежнему
отвечает журнал. Отдельный маршрут «показать телефон» остаётся для экранов,
где номера на кадре нет (приём, «Сьогодні»).

## 15. Статистика — экраны по семи кадрам

Кадры f08, f09, f17, f18, f23, f24, f29 (нумерация описи
`~/Documents/polypsy-frames`). Сервер написан в волне 5 (выше), здесь — экраны
и то, что по ходу понадобилось серверу. Пункт «Статистика» верхней полосы
ведёт теперь на `/statistics`; прежний временный адрес — подбор людей
`/cohorts` — остался в бургере под своим именем «Добір людей».

**Где что лежит** (`apps/web/src/pages/statistics/`):

| Кадр | Адрес | Что на нём |
|---|---|---|
| f08 | `/statistics` | перечень моделей — сетка перечня аналитики f10 один в один; иконки вида «діаграма / перелік» |
| f09, f18 | `/statistics/:id` | модель с одной выборкой: карточка «Фільтри» (сворачивается в полосу f18), строки отчёта, «Порівняти» |
| f29 | `/statistics/:id` | та же модель с двумя выборками рядом: «+» в строке заголовка добавляет выборку, узкая колонка плашек — разность долей |
| f17 | `/statistics/new` | форма модели: тест, варианты результата, вопросы с ответами и «ВШР», «Створити» |
| f24 | `/statistics/chart` | выборки модели чипами, «Оновити», перечень моделей, диаграмма с легендой, «Керування фільтрами»; шестерёнка |
| f23 | `/statistics/filters`, `/filters/:id` | «Створення фільтра» и «Пресети фільтрів» |

**Как прочитаны кадры** — там, где буквы кадра недостаточно:

- Модель = колонки-выборки. «Что считаем» задаёт форма f17 один раз для
  всех колонок; «по кому» — фильтры выборки на экране модели. Фильтров в
  форме нет, как на кадре; при правке структура ложится на каждую колонку,
  а их выборки остаются.
- «+» экрана модели — выборка для сравнения (f09 → f29); плашки f29 —
  разность долей правой и левой выборки.
- Чипы f24 — критерии выборки: нажатый участвует в расчёте, отпущенный
  (контурный, как «Стать» у «Пресет №124») на этот расчёт снят. Окно с
  флажками в «Керуванні фільтрами» — к каким выборкам применить пресет.
- «−» и «+» у строк и карточек: последняя строка/карточка несёт «+»,
  остальные «−» (f17, f23). «−» у названия пресета — сам пресет (у нового
  очистить форму, у сохранённого — удалить).

**Функции, которых кадры не рисуют, — куда разнесены:**

- правка модели (название, описание для перечня f08, структура, удаление,
  снятие выборки) — `/statistics/:id/edit`, та же форма f17; дверь —
  название модели на её экране;
- «Група пацієнтів» — за «+» формы пресета f23; на экране модели строка
  видна только у выборки, где группа уже задана;
- «Керувати фільтрами» и «Керувати стат. моделями» — шестерёнка f24;
- «Зберегти» на экране модели — только пока выборка или «ВШР» изменены;
  расчёт правленой выборки идёт превью и ничего не сохраняет;
- строка итога после расчёта (основание, «ВШР», фраза сервера о скрытом).

**Отступления от буквы кадра, записанные у кода:** «Порівняти» стоит и на
двух выборках (на f29 кнопки нет, а правленые фильтры иначе не пересчитать;
считать на каждую правку нельзя — каждый расчёт пишется в журнал);
«Пресети фільтрів» f23 фиолетовые, а не красные (красный на кадре — пометка
макетчика «не утверждено», опись кадров читает его так же); чип
«Населенный пункт» f24 с русской «ы» — опечатка макета; строки «Назва моделі
/ Назва повідомлення · ВщВМ · ВШР» f29 стоят недоступными с подсказкой —
связей модели с моделями и рассылками сервер не знает, «ВщВМ» кадр не
расшифровывает; наезд плашки меню на шестерню f24 не повторён.

**Решение по приватности.** Публикуется только то, что проверка
восстановимости (`lib/privacy.ts`, `pinnedAreas`) не может восстановить
внутри ОДНОГО расчёта: система строится по людям по всему ответу сразу,
все колонки вместе. Межзапросная композиция («сегодня Київ, завтра Київ
без групи») решателю не видна по построению и закрывается журналом, а не
шумом: каждый расчёт — строка `audit_log` (`stat_model.run`) с
ПРИМЕНЁННЫМИ фильтрами каждой колонки (не ссылкой на пресет — пресет правят
задним числом), методикой, версией и размером выборки без подавления.
Отвергнуто маскирование шумом: шум портит клинический отчёт каждому
читателю ради одного злоумышленника, а против усреднения повторных
запросов всё равно нужен счётчик — то есть тот же журнал. Проверка:
`statModels.test.ts`, «каждый расчёт — строка журнала с применёнными
фильтрами»; мутация «писать только методику и размер» роняет её.

**«Дыра 2» второго круга** (три колонки одного ответа с одинаковыми
фильтрами и разными разрезами одной шкалы: соседки печатают слагаемые
скрытой «решти» первой) решателем f156646 закрыта: система строится по
людям, а не по колонкам, и «Низький» одной колонки с «Високий» другой
накрывают тех же людей, что и полосы первой. Дописан тест «равные составы,
разные разрезы одной шкалы»; мутация «решать систему по колонке» роняет
его, называя двоих «Середній».

**Экран печатает скрытое прочерком — и только им.** Прочерк (U+2014) с
подписью «приховано» для диктора; разность колонок печатается только между
двумя показанными ячейками — разность показанной и скрытой была бы
скрытым, выписанным через показанное. Высота столбика диаграммы — тоже
число, поэтому скрытое столбиком не рисуется вовсе. Сторож —
`apps/web/test/statistics.test.ts` (печать, разность, пункт полосы,
маршруты, номера кадров в докблоках раздела); каждая проверка снималась
мутацией.

Смоук — `e2e/statistics.e2e.ts`: своя модель через API, перечень →
модель → свёрнутые фильтры → «Порівняти» → вторая выборка → диаграмма.
Эталонные снимки раздела снимает координатор.

**Слияние в main — с перенумерацией миграций.** Ветка несла 0081 и 0082, а
в main к тому времени стояли 0083 и 0084. Мигратор drizzle применяет только
то, что новее последней применённой записи, и на базе с 0084 таблицы
моделей и пресетов молча не создались бы. Миграции перенесены в 0085
(`stat_models`) и 0086 (`user_locality`) с отметками времени после 0084.

## 16. Волна 9: графики, аналитика тестов, сводка, третий язык

Запросы заказчика 2026-09-26, по порядку: графики пройденного теста «по
нажатию на прохождение пациента»; вкладка полной аналитики по тестам в
«Аналітиці»; «Статистика»/«Добір людей» — «экран выглядит не тронутым»;
бургер «тоже по стилистике»; «Зведення» с графиками («прохождения за
последнее время, средний уровень стресса, депрессии, тревоги»); выбор языка
«не только укр и рус, а и англ»; «Лікарі» — сортировка по відділенням и
посадам, поиск по имени, телефону и логину; на «Адміністраторах» полоса
теряла разделы.

**Общий набор графиков** — `apps/web/src/charts/clinical.tsx` (арифметика
лестницы — `charts/ladder.ts`, под тестами): линейка тяжести, профиль шкал
строками (соединён линией там, где у подряд идущих шкал одна лестница),
динамика на фоне лестницы полос с ошибкой измерения, полоса долей вместо
кольца, полосы по строкам, столбцы по времени, плитка с ходом величины.
Одиночный ряд — фиолетовым макета; тяжесть — только ступенями `--sev-*` и
всегда с подписью и формой точки. Раздел с линией 2px — `ui/section.tsx`.

**Что где:**

| Экран | Адрес | Что нового |
|---|---|---|
| Графики прохождения | `/surveys/:id/responses/:rid/charts` | вкладки «Відповіді · Графіки» над f34; результат (линейка или профиль), динамика с RCI к предыдущему замеру, вклад пунктов по ключу той версии, «як відповідав»; из карточки пациента имя теста ведёт сюда |
| Аналітика → Тести | `/analytics/tests` | тест, все/один пациент, група, період, версія; виды «Огляд · Шкали · Питання · Час · Якість · Проходження»; `/surveys/:id` перенаправляет сюда, выгрузки и двери — в шестерёнке |
| Зведення | `/` | вкладка «Огляд» вместо повтора заголовка; проходження за 30 днів / 12 тижнів; стан пацієнтів за напрямами (депресія, тривога, стрес, ПТСР, благополуччя, вигорання, алкоголь) — частка в клінічних смугах, розподіл, середній бал основної методики, хід за тижнями |
| Добір людей | `/cohorts` | «Кого шукаємо → Що вийшло → Що з ними зробити → Збережені»; населений пункт, період, вираженість, вік полными годами; поимённо с назначением и группой; «Відкрити у статистиці» |
| Лікарі / Адміністратори | `/staff`, `/admins` | вкладки; поиск по ФИО, логину и телефону (целиком и куском); фильтры відділення и посада; сортировка с группами |
| Полоса и бургер | везде | полоса всегда полная (7 пунктов), «Лікарі» подсвечены и на `/admins`; бургер в одном начертании; выбор языка — меню из трёх |

**Отступления от кадров — решения заказчика 2026-09-26, записаны у кода:**
полоса у суперадмина в разделе людей больше не сжимается до одного пункта
(f47–f50); первая вкладка стартового экрана называется «Огляд»; вкладки
«Моделі · Тести» стоят над строкой f10 и опускают её на высоту полосы
вкладок; у сотрудника в мета-строке добавлены логин, телефон, відділення и
посада (f42).

**Английский.** `Lang = "uk" | "ru" | "en"` по всему монорепо; словарь
оболочки переведён целиком (2327 записей, общий глоссарий), `en` в записи
обязателен — новая строка без перевода не компилируется. Сообщения сервера
и пуши переведены; даты — `en-GB`; пуши пишутся на языке устройства
(миграция 0087). Текст методик английского не имеет, и это осознанно (нормы
установлены для своих текстов): при английском интерфейсе пункты идут
по-украински, и прохождение говорит об этом одной строкой.

**Чего нет:** сквозные сценарии (`bunx playwright test`) и эталонные снимки
после волны не перегонялись — меняются полоса, бургер, стартовый экран и
заголовок «Аналітики»; печатные формы по-прежнему не выбирают язык; условия
по шкалам в «Доборі людей» — только числом; «Нові випадки ризику» на
«Зведенні» не выведены (другое право — `alerts.review`).
